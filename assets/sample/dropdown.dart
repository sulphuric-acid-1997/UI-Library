

Dropdown1(
                    initvalue: 'one',
                    options: const ['one', 'two'],
                    hinttext: 'select one',
                    // pass controller to access data
                  );

